DROP TABLE IF EXISTS users;


CREATE TABLE users(
    id SERIAL PRIMARY KEY,
    password VARCHAR(255) NOT NULL,
    email VARCHAR(255) NOT NULL,
    created_at TIMESTAMP NOT NULL DEFAULT NOW()
)


DROP TABLE IF EXISTS,created_
CREATE TAble posts {
    id VARCHAR(32) primary_key
}
